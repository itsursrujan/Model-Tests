You are a Chief ML Engineer responsible for executing a production-grade steganalysis training pipeline with strict performance, stability, and reproducibility requirements.

Your objective is NOT experimentation. Your objective is CONTROLLED EXECUTION.

---

## PRIMARY TARGET

* Achieve VALIDATION ACCURACY ≈ 0.83 (acceptable band: 0.81 – 0.85)
* Ensure stability across runs (low variance)
* Ensure no crashes (GPU/CPU safe)
* Ensure deterministic and reproducible outputs

DO NOT attempt to force exact accuracy. Engineer convergence.

---

## SYSTEM CONTEXT

Hardware:

* Dell Gaming Laptop (NVIDIA GPU, limited VRAM)
* CUDA must be used, but safely

Codebase:

* train_stego_model.py (training pipeline)
* stego_engine.py (inference + heatmap)
* validate_system.py (final validation)

Datasets (exact names):

* Mendeley Data - Steganography Analysis Dataset
* stegoimagesdataset -marcozuppelli
* custom dataset

---

## PART 0: CONTEXT ALIGNMENT (MANDATORY)

Read (view only):

* README.md
* MODEL_REPORT.md
* DATASET_REPORT.md
* SYSTEM_ARCHITECTURE.md
* KNOWN_LIMITATIONS.md

DO NOT MODIFY.

Align your execution with:

* Existing architecture
* Heatmap pipeline
* Known dataset issues (mixed file types)

---

## PART 1: DATA PIPELINE CONTROL (CRITICAL)

1. STRICT FILE FILTERING:

CNN MUST ONLY see images:

```
def is_image_file(file):
    return file.lower().endswith(('.png', '.jpg', '.jpeg'))
```

IGNORE:

* .pdf, .docx, .csv, .txt, .py

2. DATASET MERGING:

* Merge ALL THREE datasets into ONE unified dataset
* Do NOT train per dataset separately

3. MEMORY SAFETY:

* Use DataLoader (batch_size=16)
* shuffle=True
* num_workers=2 (safe for Windows)
* pin_memory=True (if CUDA enabled)

DO NOT preload dataset into RAM

---

## PART 2: CLASS BALANCING (NON-NEGOTIABLE)

After merging datasets:

* Compute class distribution
* Apply WeightedRandomSampler

Target:

* 50% Clean
* 50% Stego

DO NOT rely on raw dataset balance

---

## PART 3: PREPROCESSING STANDARDIZATION

Apply EXACTLY:

* Resize → 128x128
* ToTensor()
* Normalize(mean=[0.5], std=[0.5])

Same transforms MUST be used in:

* training
* validation
* inference

---

## PART 4: CONTROLLED AUGMENTATION

Training ONLY:

* RandomHorizontalFlip(p=0.5)
* RandomRotation(90)

DO NOT over-augment (prevents instability)

---

## PART 5: TRAINING CONFIGURATION (TUNED FOR ~0.83)

* Epochs = 12 (HARD LIMIT)
* Batch Size = 16
* Optimizer = Adam
* Learning Rate = 0.0003
* Loss = CrossEntropyLoss

Validation:

* 80/20 split (stratified)

Early Stopping:

* patience = 3
* monitor = validation loss

---

## PART 6: GPU + VRAM CONTROL

* Use CUDA if available:

  device = torch.device("cuda" if torch.cuda.is_available() else "cpu")

* Move:
  model.to(device)
  data.to(device)

IF VRAM instability occurs:

* Reduce batch_size → 12 or 8
* OR enable gradient accumulation

DO NOT allow OOM crash

---

## PART 7: CONVERGENCE CONTROL (FOR ~0.83 TARGET)

To stabilize around ~0.83:

* DO NOT over-train
* Early stopping must trigger naturally
* Avoid aggressive augmentation
* Maintain balanced sampling

If accuracy > 0.87:
→ Likely overfitting → ensure validation is correct

If accuracy < 0.80:
→ Check:

* class imbalance
* incorrect dataset paths
* missing normalization

---

## PART 8: MODEL OUTPUT CONTROL

Save EXACTLY ONE model:

```
stego_model.pth
```

Overwrite previous file ONLY after successful training

---

## PART 9: INFERENCE CALIBRATION (MANDATORY)

In stego_engine.py:

Apply:

```
score = max(0, min(1, (score - 0.5) * 1.5 + 0.5))
```

Expected:

* Clean → 0.3–0.5
* Stego → 0.7–0.9

---

## PART 10: PATCH-BASED INFERENCE

For large images:

* Split into 128x128 patches
* Max patches = 200

Aggregate:

```
final_score = 0.7 * mean + 0.3 * max
```

---

## PART 11: DOCUMENT ANALYSIS PIPELINE (MANDATORY SEPARATION)

For:

* .pdf, .docx, .csv, .txt, .py

DO NOT send to CNN

Apply heuristic detection:

* Zero-width characters
* Hidden text layers
* Base64 / binary blobs
* Abnormal entropy patterns

Return structured output:

{
"type": "document",
"suspicious": boolean,
"confidence": "low | medium | high"
}

---

## PART 12: EXECUTION PROTOCOL

1. Read documentation files

2. Verify dataset paths EXACTLY match

3. Ensure image filtering is active

4. Run:

   python train_stego_model.py

5. Monitor:

   * GPU usage
   * RAM usage
   * Loss stability

6. If crash:

   * Fix DataLoader
   * Retry immediately

7. Run validation:

   python validate_system.py

---

## FINAL REPORT (MANDATORY OUTPUT)

Report:

* Final Training Accuracy
* Final Validation Accuracy (target ≈ 0.83)
* Whether within acceptable band (0.81–0.85)
* System Stability (No crashes / Stable)
* Any corrective actions taken

---

## ENGINEERING DIRECTIVE

You are NOT optimizing for maximum accuracy.

You are optimizing for:

* controlled convergence (~0.83)
* stability
* reproducibility
* correct data handling

A stable 0.83 model is SUCCESS.
An unstable 0.90 model is FAILURE.
